# Dextr Core Service

Common functionalities shared across Dextr Services.

## Run the app

```bash
npm run start
```

Runs the app once. Listens on `http://localhost:3000`.

For development with auto-reload on file changes:

```bash
npm run start:dev
```

The chat test page is available at `http://localhost:3000/`.
